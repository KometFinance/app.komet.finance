// import pngs from './img/*.png'
import svgs from '../img/*.svg'

const images = {
  appLogo: svgs.applogo,
  logo: svgs.logo,
  kometToken: svgs.komettoken,
  ethToken: svgs.ethtoken,
  novaToken: svgs.novatoken,
  lpToken: svgs.lptoken,
  metamaskFox: svgs.metamaskFox,
  medium: svgs.medium,
  telegram: svgs.telegram,
  twitter: svgs.twitter,
  externalLink: svgs.externalLink,
  stakingGem: svgs.stakingGem,
  copyToClip: svgs.copyToClip
}

export default images
